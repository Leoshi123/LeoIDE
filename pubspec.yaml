name: leoide
description: LeoIDE - IDE móvil personal de alto rendimiento. Compila C/C++, Python, PHP y Web nativamente en Android.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
